complete -c labwc_wuwa_daily -l headless -d "Use headless WLR backend"
complete -c labwc_wuwa_daily -l enable-dx11 -d "Enable DX11 mode"
complete -c labwc_wuwa_daily -l enable-wayvnc -d "Enable wayVNC for headless mode (default)"
complete -c labwc_wuwa_daily -l disable-wayvnc -d "Disable wayVNC for headless mode"
